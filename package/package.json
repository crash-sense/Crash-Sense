{
  "name": "crashsense",
  "version": "1.0.0",
  "description": "Runtime AI debugger middleware for Express.js",
  "main": "src/index.js",
  "files": ["src", "README.md"],
  "license": "MIT",
  "engines": { "node": ">=18.0.0" },
  "scripts": {
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "lint": "eslint src test",
    "format": "prettier --write src test"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "express": "^4.18.0",
    "jest": "^29.0.0",
    "supertest": "^6.3.0",
    "eslint": "^8.57.0",
    "prettier": "^3.2.0"
  },
  "peerDependencies": {
    "express": ">=4.0.0"
  },
  "jest": {
    "testEnvironment": "node"
  }
}